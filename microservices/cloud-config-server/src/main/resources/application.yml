server :
  port : 7100

spring :
  application :
    name : CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/NirdeshMohan/config-server
          clone-0n-start: true
